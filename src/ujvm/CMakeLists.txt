file(GLOB_RECURSE SOURCES LIST_DIRECTORIES true *.hpp *.h *.cpp)
set(SOURCES ${SOURCES})
include_directories(../shared)
#message(${SOURCES})
add_library(ujvm SHARED ${SOURCES})
target_link_libraries(ujvm shared classfile)